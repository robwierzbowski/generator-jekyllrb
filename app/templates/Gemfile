source "http://rubygems.org"

gem 'jekyll', '~>1.3.0'
gem '<%= jekMkd %>'<% if (cssPre === 'sass') { %>
gem 'sass', '>=3.2'<% } %><%  if (cssPre === 'compass') { %>
gem 'compass', '>=0.12'<%     } %>
